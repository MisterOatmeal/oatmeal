# Operating System files
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini
$RECYCLE.BIN/

# Editor/IDE specific files
# Visual Studio Code
.vscode/

# Sublime Text
*.sublime-workspace
*.sublime-project

# Lua-specific temporary or generated files
# If you use a Lua linter that generates temporary files
# *.lua.lint

# Dependency management (if you use something like LuaRocks, though less common for FFXIV scripts)
# rocks/
# build/

# Log files
*.log
logs/
debug.log

# Temporary files generated by the script or development process
*.tmp
*~ # Emacs temporary files
#*# # Emacs lock files

# User-specific configuration files that should not be versioned
# If your script has a configurable file that each user would modify locally
# For example, if you provide a `config_example.lua` and expect users to copy it to `config.lua`
# and modify it, then `config.lua` might be ignored.
config.lua
user_settings.lua

# SomethingNeedDoing (SND) specific files (common patterns)
# If SND generates specific debug outputs or temporary data in your script directory
SND_DebugOutput.txt
SND_TempData/
SomethingNeedDoing_logs/
SND
